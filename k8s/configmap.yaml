apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  analytics.window_size: "50"
  analytics.threshold: "2.0"
  redis.host: "redis"
  redis.port: "6379"
  redis.max_memory: "256mb"
  redis.eviction_policy: "allkeys-lru"
  cache.ttl_minutes: "5"